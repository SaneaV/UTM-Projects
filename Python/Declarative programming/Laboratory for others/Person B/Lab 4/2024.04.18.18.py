# Решите дифференциальное уравнение Бернулли 𝑥 * 𝑑𝑓(𝑥) / 𝑑𝑥 + 𝑓(𝑥) − 𝑓(𝑥)^2 = 0. Решите
# то же уравнение, используя hint='Bernoulli'. Что вы замечаете?

# Импорт модуля sympy
import sympy as sp

# Определение символов x и f(x)
x = sp.symbols('x')
f = sp.Function('f')(x)

# Определение уравнения дифференциального уравнения Бернулли
eq = x * sp.diff(f, x) + f - f**2

# Решение уравнения без подсказки
solution_no_hint = sp.dsolve(eq, f)

# Решение уравнения с подсказкой 'Bernoulli'
solution_with_hint = sp.dsolve(eq, f, hint='Bernoulli')

# Вывод результатов
print("Решение без подсказки:", solution_no_hint)
print("Решение с подсказкой 'Bernoulli':", solution_with_hint)

# При решении уравнения с использованием подсказки 'Bernoulli' мы получаем эквивалентное решение. 
# Это происходит потому, что уравнение Бернулли в данном случае уже является уравнением Бернулли, 
# и использование подсказки не влияет на результат.